import { LocaleHelperService } from "./locale-helper.service";
export declare class DateIOService {
    private _localeHelperService;
    cldrLocaleDateFormat: string;
    private localeDisplayFormat;
    constructor(_localeHelperService: LocaleHelperService);
    private initializeLocaleDisplayFormat();
    toLocaleDisplayFormatString(date: Date): string;
    readonly placeholderText: string;
    /**
     * Detects if the input date string contains any one
     * of the separators from the SEPARTORS array.
     */
    private detectSeparator(date);
    /**
     * Checks if the string is a non negative number.
     * Credit: https://stackoverflow.com/a/24457420/8960224
     */
    private isNonNegativeNumber(num);
    /**
     * Checks if the date parts are numbers
     */
    private areDatePartsNumbers(dateParts);
    /**
     * Checks if the month entered by the user is valid or not.
     * Note: Month is 0 based.
     */
    private isValidMonth(month);
    /**
     * Checks if the date is valid depending on the year and month provided.
     */
    private isValidDate(year, month, date);
    /**
     * Validates the parameters provided and returns the date.
     * If the parameters are not
     * valid then return null.
     * NOTE: (Month here is 1 based since the user has provided that as an input)
     */
    private validateAndGetDate(year, month, date);
    /**
     * Checks if the input provided by the user is valid.
     */
    isValidInput(date: string): Date;
}
